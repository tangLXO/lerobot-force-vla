# X518 400 Hz 稳定化验证（2026-09-15）

## 范围和实现

实现基于 `26334ea8`。验证环境为 Windows、Python 3.12.13；
X518 `192.168.1.100:502` 报告 400 Hz、kg、小数位 4；语义输出转换为 N。
只读检查使用 COM13 SO101 follower、COM24 SO101 leader、两路 640×480@30 DSHOW 相机。
这些录制不发送运动命令，不写扭矩或设备频率寄存器，不上传 Hub，不作为训练示范。

交付内容：

- X518 默认 `spawn` 独立进程；显式 `acquisition_mode="thread"` 可作兼容和对照。
- 有界 IPC（默认 1,024）、独立故障通知、私有传输序号校验、同通道订阅屏障。
- required Recorder 租约确认后禁止重连，结束屏障先于 Recorder 排空；进程或完整性故障阻止提交。
- 绝对截止时间轮询，统计并跳过过期槽位；不补造样本，不删除数值相同的合法响应。
- `CycleTimer.wait()` 使用释放 GIL 的睡眠与末尾 1 ms 调度让出，保留原截止时间和统计语义。
- 子进程响应时间与父进程可用时间分开，按父进程 arrival 做因果选择；tactile 和 Sync 复用一次选择。
- 启动失败、阻塞读取、进程死亡、父进程退出和停止超时均有清理路径；资源未释放时禁止重连复用。
- 只读运行诊断报告频率、延迟、年龄、队列峰值、无效样本、传输缺口和超期槽位。

Sidecar v2、Raw/Sync Arrow schema、存储布局和事务 journal 版本不变。
历史 `actual_sample_rate_hz` 仍按 arrival 统计；响应频率只进入运行报告。
这里的 400 Hz 指真实 Modbus 响应及完整记录，不承诺每次 ADC 转换唯一。

## 验收方法

有效响应频率为 `(有效响应数 - 1) / (末次响应时间 - 首次响应时间)`。
完整录制只取第一帧到最后一帧的 `frame_anchor_ns` 区间。
报告分位数使用整个测量区间；驱动运行快照的分位数仅覆盖最近 4,096 个值。
频率不足保留录制并判定性能失败；完整性故障按既有事务规则保留失败证据。

门槛：响应 396–404 Hz，间隔 P99 ≤4.5 ms，发布延迟 P99 ≤10 ms，
主帧所选年龄 P99 ≤15 ms 且最大值 ≤配置的 100 ms，完整录制主帧 29.7–30.3 Hz；
无效样本、传输缺口、IPC/Recorder 溢出必须为 0。

### 实施前模拟对照

以下为任务中提供的实施前结果：同一模拟设备、0.5 ms 模拟 I/O、约 4 秒；
它们用于定位等待逻辑与 GIL 竞争，不计入本次真机 Gate。

| 采集方式   | 主循环等待 | 响应 Hz | 间隔 >4.5 ms 次数 |
| ---------- | ---------- | ------- | ----------------- |
| 同进程线程 | 普通 sleep | 399.88  | 1                 |
| 同进程线程 | 原空转等待 | 359.30  | 119               |
| 独立进程   | 原空转等待 | 399.93  | 0                 |

本次新增的真实 spawn / 本地 Modbus 模拟服务测试用于验证故障、因果关系和生命周期，
没有把测试执行速度作为性能保证；实施后性能以以下本机真机测量为准。

## Gate 2：三档负载，各 3 × 60 秒

九次均通过。所有无效样本、序号缺口、IPC/Recorder 溢出均为 0。

| 负载             | 次数 | 响应 Hz | 间隔 P99 ms | 发布延迟 P99 ms | 主帧 Hz | 所选年龄 P99 ms |
| ---------------- | ---- | ------- | ----------- | --------------- | ------- | --------------- |
| X518 单独        | 1    | 399.965 | 2.978       | 0.499           | —       | —               |
| X518 单独        | 2    | 399.999 | 2.979       | 0.526           | —       | —               |
| X518 单独        | 3    | 399.986 | 2.981       | 0.519           | —       | —               |
| 两相机＋双臂只读 | 1    | 399.901 | 2.944       | 0.447           | —       | 2.849           |
| 两相机＋双臂只读 | 2    | 400.002 | 2.966       | 0.442           | —       | 2.936           |
| 两相机＋双臂只读 | 3    | 399.985 | 2.943       | 0.436           | —       | 2.820           |
| 完整录制         | 1    | 399.965 | 2.905       | 0.508           | 29.942  | 2.833           |
| 完整录制         | 2    | 399.897 | 2.910       | 0.508           | 29.944  | 2.865           |
| 完整录制         | 3    | 399.984 | 2.914       | 0.526           | 29.962  | 2.883           |

完整录制共 5,392 主帧，10,784 视频帧全部解码；每帧 tactile/window/Sync 引用一致。
三次完整录制退出后的 `resources_released` 均为 true。

证据位于本地 `outputs/x518_400hz_20260915/`：

- `gate2_standalone_1.json` 至 `gate2_standalone_3.json`
- `gate2_observe_v2_1/hardware_validation.json` 至 `gate2_observe_v2_3/hardware_validation.json`
- `gate2_collection_1/hardware_validation.json` 至 `gate2_collection_3/hardware_validation.json`
- 每次完整录制的 `acquisition_runtime.json` 保存队列、内存和清理证据。

### 保留的调试结果

- 初次 10 秒普通 sleep 完整录制响应 399.991 Hz，但主帧 29.6977 Hz，低于门槛；
  `collection_quick` 保留该失败。加入 `sleep(0)` 尾部让出后，`collection_quick_v2` 为
  399.909 Hz、29.9959 主帧 Hz，通过短检查。
- 初次组合负载的第二轮在停止时遇到 Windows 管道 `poll()` 的 `BrokenPipeError`，
  修复后用 `gate2_observe_v2_*` 完整重跑三次；中断轮次不计为通过。
- 组合负载出现过约 78.4 ms 最大响应到发布延迟、约 66.8 ms 最大选样年龄，
  P99 均满足门槛且未超过配置年龄。这说明桌面调度仍有偶发抖动，验收不等于硬实时保证。

## Gate 3：持续录制

| 路径         | 时长  | 响应 Hz | 间隔 P99 ms | 发布延迟 P99 ms | 主帧 Hz | 所选年龄 P99 / 最大 ms | 结果 |
| ------------ | ----- | ------- | ----------- | --------------- | ------- | ---------------------- | ---- |
| 普通图像写入 | 600 s | 399.942 | 2.909       | 0.544           | 29.940  | 2.896 / 9.194          | 通过 |
| 流式编码     | 600 s | 399.937 | 2.958       | 0.926           | 29.955  | 2.923 / 67.721         | 通过 |

普通写入：17,964 主帧，测量区间内 239,955 个有效响应，35,928 视频帧全部解码，
全量 Raw/Sync 验证和逐帧 tactile 引用一致性通过；无效、缺口、溢出全部为 0。
采集区间 IPC 峰值 7/1,024，Recorder 峰值 599/800；退出后资源释放。
每 5 秒采样的 120 个内存快照中，采集子进程从 408.44 MiB 到 408.52 MiB；
父进程从 460.42 MiB 到 497.13 MiB，包含主帧元数据和图像写入。
Sensor 队列、History 和运行分位数均有固定容量，不承诺整个 LeRobot 主数据集的内存与时长无关。
证据：`gate3_normal_600/hardware_validation.json` 和 `acquisition_runtime.json`。

离线编码和提交发生在测量区间之后，约耗时 3.5 分钟。该时段的驱动 lifetime 诊断包含
额外轮询和更高队列峰值；它们没有被用于上述录制频率，且全程无溢出、故障。

流式编码：17,974 主帧，测量区间内 239,959 个有效响应，35,948 视频帧全部解码，
全量 Raw/Sync 验证和逐帧 tactile 引用通过；无效、缺口、溢出全部为 0。
IPC 峰值 30/1,024，Recorder 峰值 37/800；退出后资源释放。
采集子进程从 408.38 MiB 到 408.44 MiB；父进程包含编码器，从 458.63 MiB 到
672.90 MiB，峰值 673.71 MiB。最大选样年龄 67.721 ms 未超过 100 ms 配置，
但仍说明偶发调度停顿存在。证据：`gate3_streaming_600/hardware_validation.json`。

因此，**Gate 2 和两条 Gate 3 的本机只读性能验收通过**。本结果针对上述设备、相机和编码配置，
不等于其他主机、机械臂驱动、运动控制或更高分辨率下的硬实时保证。

## Gate 1：自动化验证与质量检查

- 最终 Sensor、Recorder、队列、计时器、wrapper 回归：**195 passed**，44.46 s；
  `targeted_final.log` / `targeted_final.xml`。
- 最终事务恢复、Reader、窗口 oracle、批量窗口、Hub、控制循环和 rollout 集成回归：
  **1,305 passed、1 skipped**，405.95 s；`integration_final.log` / `integration_final.xml`。
  两组共 **1,500 passed、1 skipped**。跳过项需要本 Windows 主机未授予的文件符号链接权限。
- 随后补充“已完成订阅屏障的句柄必须交还”的故障竞态回归；进程模块 **25 passed**，
  28.49 s，`process_final.log`。其中 24 项与前述定向测试重叠；相关回归去重后为
  **1,501 passed、1 skipped**。
- 收尾复查进程模块与可选依赖入口：**29 passed**，26.65 s；
  `final_smoke.log` / `final_smoke.xml`。这是重复验证，不另加到上述相关测试总数。
- 故障测试使用真实 Windows spawn 和本地 Modbus 服务，覆盖延迟交接因果性、订阅尾部、
  容量外溢出、重复/缺口、required 断连、重连与租约竞争、启动失败、强制停止、所有权保留、
  父进程突然退出、两 episode 提交及 Reader tree/mtime 不变。
- 变更文件 pre-commit 全部通过：Ruff、格式、Mypy、Bandit、拼写、泄密检查等；`quality.log`。
- doctest 列表、函数和配置文档检查通过；未安装的 Unitree 可选依赖按检查器规则跳过。
- 现有短真机入口的重录和续录流程通过：1 个 ABORTED、2 个 COMMITTED，
  全量 Raw/Sync、360 个视频帧及逐帧 tactile 引用一致，退出后资源释放。
  证据：`rerecord_resume/hardware_validation.json`。
- 全套 `pytest tests --maxfail=10 --timeout=120` 已尝试：**96 passed、10 failed、19 skipped** 后停止。
  失败均在现有 OpenCV PNG 测试，包含四通道 fixture 与三通道期望不匹配及本机 decoder 不可用；
  `full_suite.log` / `full_suite.xml`。这不是全套通过。

全套无停止上限尝试在约 56% 进入 CPU 大模型对照，内存超过 20 GB，长时间未完成，
人工中断并保留 `full_runnable.log`，不计为完整通过。
后续按仓库 `CI=true` 跳过规则运行其余模块，并复用上述已完成的 Sensor/事务组。
为限制大模型和数据集下载，本次主动设置 `HF_HUB_OFFLINE=1`，将 `TORCH_HOME` 指向工作区缓存；
因此下面的离线失败代表本次验证范围限制，不代表网络故障。

剩余模块完成结果为 **2,733 passed、57 failed、208 skipped、15 errors、3 deselected**，
376.26 s，见 `remaining_ci.log` / `remaining_ci.xml`：

| 分类                           | 失败 / 错误数 | 观察到的原因                                |
| ------------------------------ | ------------- | ------------------------------------------- |
| OpenCV PNG 测试                | 32 failed     | 图像 fixture 通道不匹配、解码或读取线程失败 |
| Dataset 路径测试               | 1 failed      | 测试按 POSIX 路径分隔符匹配 Windows 路径    |
| 数据集、预训练模型测试         | 21 failed     | 离线模式及缺少本地缓存                      |
| Replay buffer 图像增强         | 1 failed      | 本机缺少 `cl` 编译器                        |
| Checkpoint 符号链接            | 2 failed      | Windows 未授予符号链接权限                  |
| Relative-action / XVLA fixture | 15 errors     | 离线模式及缺少本地模型、数据集缓存          |

三个可能阻塞的用例单独执行：大参数传输本次 **1 passed**；
两个异步 replay-buffer 迭代器分别在 20 秒超时，均卡在队列 `get()`，属于未解决的全套限制。
证据为 `isolated_test_*.log`。上述失败位于本次未修改的模块；没有逐一在干净基线复现，
不将它们全部宣称为已证明的历史故障。

结论：与此次改动直接相关的确定性、进程故障和集成回归通过，真机性能 Gate 2/3 通过；
**全仓回归未全绿**，上述失败、错误和两个超时仍保留在报告中。

## 复现入口

见 [集成说明第 14 节](SENSOR_INTEGRATION_GUIDE.md#14-当前-x518-参考实例)。
所有脚本沿用现有示例；未恢复已删除的相对姿态录制脚本。
报告、日志、视频和 Raw 文件保留在 `outputs/`，不加入源代码目录或 Git。
